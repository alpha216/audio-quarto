# macOS
.DS_Store
.AppleDouble
.LSOverride

# Quarto — cache, preview, and incremental compile state
.quarto/

# Quarto website build output (default project output dir)
_site/

# Local env / secrets (if you add tooling later)
.env
.env.*
!.env.example
